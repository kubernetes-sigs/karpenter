name: "Scheduling Benchmarking"

on:
  pull_request:
    paths:
      - "**.go"
    branches:
      - main

jobs:
  # index pins the artifact name each leg uploads, and the compare job below keys
  # off the same index. timeoutMinutes is a per-leg wall clock ceiling. Measured
  # on ubuntu-latest at -count=6, the disruption leg's Run Test step took 37m40s
  # and the scheduling leg's took 16m22s; at -count=4 those come to roughly 26
  # and 12 minutes. The ceilings below sit above those with room for a slower
  # runner, and are deliberately tight enough that a hang is caught in minutes
  # rather than at GitHub's 360 minute default.
  before:
    name: Before PR
    strategy:
      fail-fast: false
      matrix:
        include:
          - index: 0
            path: "pkg/controllers/provisioning/scheduling"
            timeoutMinutes: 30
          - index: 1
            path: "pkg/controllers/disruption"
            timeoutMinutes: 45
    uses: ./.github/workflows/run-bench-test.yaml
    secrets: inherit
    with:
      path: ${{ matrix.path }}
      runName: before-${{ matrix.index }}
      githubSha: ${{ github.event.pull_request.base.sha }}
      timeoutMinutes: ${{ matrix.timeoutMinutes }}
  after:
    name: After PR
    strategy:
      fail-fast: false
      matrix:
        include:
          - index: 0
            path: "pkg/controllers/provisioning/scheduling"
            timeoutMinutes: 30
          - index: 1
            path: "pkg/controllers/disruption"
            timeoutMinutes: 45
    uses: ./.github/workflows/run-bench-test.yaml
    secrets: inherit
    with:
      path: ${{ matrix.path }}
      runName: after-${{ matrix.index }}
      githubSha: ${{ github.sha }}
      timeoutMinutes: ${{ matrix.timeoutMinutes }}
  compare:
    name: Compare Results
    needs: [before, after]
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - index: 0
            name: scheduling
          - index: 1
            name: consolidation
    steps:
      - uses: actions/download-artifact@fa0a91b85d4f404e444e00e005971372dc801d16 # v4.1.8
        continue-on-error: true
        with:
          name: before-${{ matrix.index }}
          path: before
      - uses: actions/download-artifact@fa0a91b85d4f404e444e00e005971372dc801d16 # v4.1.8
        continue-on-error: true
        with:
          name: after-${{ matrix.index }}
          path: after
      - name: Check artifacts
        id: check
        run: |
          if [ ! -f after/results.txt ]; then
            echo "::error::Missing after/results.txt: this PR's benchmark job did not produce results."
            exit 1
          fi
          if [ ! -f before/results.txt ]; then
            echo "::warning::Missing before/results.txt: skipping comparison (base benchmark job did not produce results, or fork PR without upload permissions)."
            echo "skip=true" >> "$GITHUB_OUTPUT"
          else
            echo "skip=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Install benchstat
        if: steps.check.outputs.skip == 'false'
        run: go install golang.org/x/perf/cmd/benchstat@latest
      - name: Compare benchmarks
        if: steps.check.outputs.skip == 'false'
        id: benchstat
        shell: bash
        run: |
          RESULT=$(benchstat before/results.txt after/results.txt 2>&1 || true)
          {
            echo "result<<EOF"
            echo "$RESULT"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

          # Gate on allocs/op only, at >=20% delta + p<0.05.
          # allocs/op is deterministic from code paths and portable across
          # runners; sec/op noise at n=6 regularly exceeds any threshold below
          # ~200% on identical code, so a sec/op threshold cannot separate
          # real regressions from runner variance without also masking them.
          ALLOCS=$(benchstat -filter '.unit:allocs/op' before/results.txt after/results.txt 2>&1 || true)
          if echo "$ALLOCS" | grep -E '\+[2-9][0-9]\.[0-9]+%|\+[1-9][0-9]{2,}\.[0-9]+%' | grep -v '~' | grep -qE 'p=0\.0[0-4]'; then
            REGRESSED=true
          else
            REGRESSED=false
          fi
          echo "regression=$REGRESSED" >> "$GITHUB_OUTPUT"
      - name: Post PR comment
        if: always() && steps.check.outputs.skip == 'false' && github.event.pull_request.head.repo.full_name == github.repository
        uses: marocchino/sticky-pull-request-comment@67d0dec7b07ed060a405f9b2a64b8ab319fdd7db # v2.9.2
        with:
          header: bench-${{ matrix.name }}
          message: |
            ## ${{ matrix.name }} benchmark comparison

            <details>
            <summary>benchstat output</summary>

            ```
            ${{ steps.benchstat.outputs.result }}
            ```

            </details>

            ${{ steps.benchstat.outputs.regression == 'true' && '⚠️ **Statistically significant allocs/op regression detected (>20%, p<0.05).** Please investigate before merging.' || '✅ No significant allocs/op regression detected.' }}
      - name: Fail on regression
        if: steps.benchstat.outputs.regression == 'true'
        run: |
          echo "::error::Performance regression detected in ${{ matrix.name }} benchmarks"
          exit 1