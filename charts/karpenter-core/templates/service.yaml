apiVersion: v1
kind: Service
metadata:
  name: {{ include "karpenter-core.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "karpenter-core.labels" . | nindent 4 }}
  {{- with .Values.additionalAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: ClusterIP
  ports:
    - name: http-metrics
      port: 8080
      targetPort: http-metrics
      protocol: TCP
    - name: https-webhook
      port: 443
      targetPort: https-webhook
      protocol: TCP
  selector:
    {{- include "karpenter-core.selectorLabels" . | nindent 4 }}
